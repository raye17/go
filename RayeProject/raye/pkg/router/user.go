package router

import "github.com/gin-gonic/gin"

func userRouter(r *gin.RouterGroup) {
	//noAuth := r.Group("")
	//auth := r.Group("")
	//user:= noAuth.Group("user")
	{
		//user.POST("/", CreateUser)
	}
}
